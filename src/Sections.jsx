const Sections = (props) => {
    function addSection() {

    };
    return(
        <section>
            <h2>Sections</h2>
            <CreateSection {...props} />
        </section>
    );
};

export default Sections;
